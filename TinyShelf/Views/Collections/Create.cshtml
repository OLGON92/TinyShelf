@{
    ViewData["Title"] = "Create Collection";
}

@model TinyShelf.Models.Collection

<h2>Create a new Collection</h2>
@using (Html.BeginForm())
{
  @Html.Label("Title:")<br>
  @Html.TextBoxFor(model => model.Name, new { @class = "allforms", @required = "required" })<br>
  <input type="submit" value="Add" class="login-button">
}